{
  "experiment": "MF0-C",
  "title": "causal formation with a bounded learned controller",
  "spec_source": "Codex [173]/[174]/[176]/[177]；`MF0_DESIGN.md` 為設計閘",
  "status": "v4 — controller compute 已鎖；目前授權範圍 B（P0 一次訓練）",
  "why_not_MF0": {
    "point": "**這不是 `MF-0` 的一個 world，是不同的研究問題**（Codex [177]）",
    "detail": "`π_content*` 需要 prefix 的 entity counts，而 `MF-0` 的 gate 只拿得到 `W=4` 個 raw event。把 count vector **外接**給 learner，等於**手工把 focus 判據交給它**，而那不在 `W=4` 內。",
    "consequence": "研究問題從 **admission-only** 變成 **admission ＋ working-state**。",
    "alternative_if_declined": "若不引入 controller-state 這個新主題，**正確的替代不是偷偷傳 counts**，而是**放棄本實驗、轉 `RWA-0`**。"
  },
  "scope": {
    "is": "在一個受控的 latent-topic／count task 上，量 **reward 是否讓一個 bounded neural controller 從頻率統計形成可用的 retention priority**",
    "is_not": [
      "自然對話記憶",
      "純 admission（本關含 learned working-state）",
      "canonicalization／read-write agreement／selection",
      "overwrite／version（EC-1 已封，且本 world 明令禁止同 key rewrite）"
    ],
    "claim_limit": "PASS 只可稱「task loss 學到 **controller ＋ admission**」，**不得**偷稱純 admission"
  },
  "controller": {
    "state": "`h ∈ FP16^16`，**每 session reset**，runtime state **恰 32 bytes**",
    "quantization": "**每一步 forward 之後立即 cast／quantize 為 FP16**；**不得**在兩步之間保留 FP32 master／cached state",
    "fixed_once": "**不做 width／bytes sweep**。此 size 若 FAIL，即**封存這個 `MF0-C` 配方**",
    "rationale": "16 維能表達 16 個 entity 的**低精度**統計，卻遠小於 64 條 record／value stream —— **它不是「足以解題」的保證**",
    "input": "`u_t = onehot(goal:3) || onehot(entity:16) || onehot(attribute:12)` = **31 維**",
    "forbidden_in_input": [
      "value",
      "absolute position",
      "hash",
      "外部 count",
      "focus 標記",
      "future query",
      "`W=4` 以外的 raw history"
    ],
    "arch": "單層 `GRUCell(31, 16)`；`h_t = fp16(GRUCell(u_t, h_{t-1}))`",
    "priority_head": "**單一 affine head** `a_t = b + w_u^T u_t + w_h^T h_t`",
    "policy": "其餘 reservation／top-B 依既鎖規格",
    "canonical_ok": "本關**不測 canonicalization**，故可用 current record 的 canonical entity／attr；**但未給 count 或 focus**",
    "at_query_time": "**不輸出、不交付 value**",
    "w4": "`W=4` 仍是 raw workspace 上限，**controller 不得偷看**",
    "h0_ablation": "**每步以 `GRUCell(u_t, 0)` 算 head、隨即丟棄**，禁止任何跨步 `h`。⚠️ **不得**把整個 GRU head 歸零 —— 那會混入一個**不同的 per-event capacity 差異**，測到的就不是 recurrent state 的貢獻"
  },
  "generator_frozen": {
    "note": "Codex [177] Q1：**不用可調機率**，鎖精確且**與 `H` 無關**的 count profile",
    "E": 16,
    "N": 64,
    "profile": "隨機選一個 **focus entity**，對它產生**恰好 12** 筆**彼此不同 attribute** 的 record（**完整 12-attr vocabulary**）；其餘 15 個 entity 中，**7 個各 4 筆**、**8 個各 3 筆**，各自 attribute 互異。`12 + 7×4 + 8×3 = 64`。",
    "order": "事件順序**均勻打散**",
    "why_12": "`12` 來自**完整的 attribute vocabulary**，且 `12 > B=8` —— **不是看 `H` 選出來的比例**。這既讓 focus 頻率可由 prefix 統計推斷，又令「只看 focus 不看 goal」仍有**超過 `B`** 的候選。",
    "no_rewrite": "**每 `(entity, attr)` 在 session 內只出現一次**（Codex [177] Q3）—— 沒有 value／version survival 的未來資訊可被 reference 偷用",
    "query_rule": "**hard 合取**：`attr ∈ goal class` **AND** `entity == focus`（**不採加權**）",
    "Q": 16,
    "W0_control": "**對稱的負對照**：同樣的 surface（goal token、同一 count profile），但 query 與 `(goal, focus)` **獨立**；所有 non-oracle policy 的期望應相等，任何穩定優勢**先查 leakage**"
  },
  "references": {
    "note": "四個 reference ＋ 一個上界。**防灌水的方法不是引入 mixing weight，而是補上 single-factor reference**（Codex [177] Q2）",
    "random": "reservoir，`B` 個 slot",
    "recency": "線上，新的擠掉最舊的",
    "goal_only": "**只讀**可見 goal class；不讀 future query／count／value；同分以事前固定的 canonical-key hash 打破",
    "focus_only": "**只讀** `P(F=e | prefix)`；**不讀** goal attribute",
    "pi_content_star": "`priority = 1[attr ∈ goal] × P(F = e | prefix **含當前 event**)`；同一 online top-B ／同一 tie rule；**不得看 future**。這是 offline 的 Bayes-causal reference，**不可部署**",
    "oracle": "事後 future-use count 的 top-B。**只報**不可部署上界與剩餘 query-sampling variance",
    "frozen_predictive_surprisal": "見 `frozen_predictive_surprisal` 節（canonical，非文字 LM）"
  },
  "headroom_precondition": {
    "H_g": "U(pi_content_star) − U(goal_only)",
    "H_f": "U(pi_content_star) − U(focus_only)",
    "H": "**min(H_g, H_f)**",
    "rule": "**只有每一個 single-factor 都有可量空間才開訓練** —— 三 seed 的 `H_g` 與 `H_f` 若任一的 95% CI 上限 `<= 5pp`，標為 `causally non-discriminating`，**不訓練**",
    "why_min": "若只相對 `goal-only` 量 headroom，learned gate 可以**只學會 focus 這一個因子**就過關（反之亦然）。取 `min` 逼它兩個都要學"
  },
  "primary_gate": {
    "per_seed": true,
    "criteria": [
      "`U(learned) − U(goal_only) >= max(5pp, 0.5H)`，paired bootstrap 95% CI 下限 `> 0`",
      "`U(learned) − U(focus_only) >= max(5pp, 0.5H)`，paired bootstrap 95% CI 下限 `> 0`",
      "對 `frozen predictive-surprisal` 的 paired CI 下限 `> 0`"
    ],
    "no_pooling": "三個 seed **各自**都要過；**不得** pooled",
    "also_report": [
      "oracle-gap（僅描述）",
      "dropped-record correct abstain",
      "真正 absent-key 的 0 delivery",
      "`h=0` state-ablation 的落差"
    ]
  },
  "frozen_predictive_surprisal": {
    "name": "frozen **canonical** predictive-surprisal",
    "not_a_text_lm": "**不是文字 LM**（Codex [178] 的 scope-alignment）：value 是 iid，且 controller 也讀不到 value；**把隨機 digit 的 NLL 混進 surprise 只是人為加噪，並不更像 Titans**",
    "rename_note": "**不可叫 Titans-style** —— 真正的 Titans 有 online associative-memory update，frozen 預測誤差不能代稱",
    "P0_inputs": "可見 goal ＋ **最多前 `W=4` 條** canonical `(entity, attr)`；**每次 scoring window 從零狀態重跑**，**絕不跨四條保留 state**",
    "P0_outputs": "預測 current entity 的 **16-way** 與 attr 的 **12-way** categorical head",
    "s_t": "`s_t = 0.5 [ -log p0(e_t | ·) - log p0(a_t | ·) ]`；同一 reservoir／top-B／hash tie rule",
    "P0_arch": "單層 `GRUCell(28, 16)`（event input = `onehot(entity:16) || onehot(attr:12)`；**goal 的 3-way onehot 接到兩個 output head**，**無其他 embedding／tokenizer**），兩個 softmax head",
    "P0_corpus": "**獨立預生成 100,000 個 event-only stream**，generator 正是 frozen `W0`／`MF0-C` 的 surface／count profile，**但無** query／value／admission／reward／Store 欄位",
    "P0_corpus_seed": 2026081301,
    "P0_corpus_fingerprint": "a4b73fa74b11ff17",
    "P0_train_seed": 2026081302,
    "P0_optimizer": "AdamW(lr=1e-3, betas=(0.9, 0.999), weight_decay=0.01)",
    "P0_batch": 512,
    "P0_batch_note": "**隨機 causal window**",
    "P0_updates": 20000,
    "P0_no_selection": "**不設** validation／early stop／checkpoint selection —— **僅取 final step**",
    "disjoint": "`MF0-C` 的 train／eval artifact 必須與此 corpus 的 **episode-id disjoint**",
    "after": "P0 結束即 **freeze**；**不共享參數、不更新**",
    "claim_limit": "這是一個**明確且受限的 predictive baseline**；**它的輸贏不能評論 Titans**",
    "P0_final_checkpoint_fingerprint_recorded_after_run": "**只能在固定的第 20,000 次 final update 之後如實記錄**。⚠️ **不可**當作訓練前 requirement，**不可**作為 checkpoint-selection 條件",
    "manifest": "兩個 fingerprint 都須連同 **builder／trainer 的 code revision、完整 config、檔案 bytes hash** 寫入 manifest",
    "P0_corpus_fingerprint_note": "**已於 artifact phase 生成後填入（required commit）**；phase 完成後**不得再生 corpus 或換 seed**"
  },
  "stopping_rule": {
    "order": [
      "**interface-only review（本輪）**",
      "phase-0（不訓練）",
      "P0 訓練（規格已鎖，**不得先跑看效果再定**）",
      "H audit（不訓練 gate）",
      "唯有 `H_g` 與 `H_f` **各自**有空間才訓練 gate",
      "一次三-seed run"
    ],
    "on_fail": "記 `MF0-C FAIL` 並依歸因分類；**不得**用 `W0`、`oracle-gap` 或單一 factor 的優勢補過",
    "on_pass": "只可稱「在此受控 latent-topic/count world，future task reward 讓 bounded controller 學出可用的 retention priority」；**不得**升格為自然記憶或一般 formation"
  },
  "assertions_required": [
    "controller 的 runtime state **恰 32 bytes**（`h.dtype == float16 and h.numel() == 16`），逐步 assert",
    "兩步之間**不存在** FP32 master／cached state",
    "controller 的 input 張量維度 **恰 31**，且不含 value／position／hash／count",
    "`h=0` ablation 走的是 `GRUCell(u_t, 0)` 而**非** head 歸零",
    "`P0` 的 scoring window **每次從零狀態重跑**",
    "`P0` corpus 與 checkpoint 的 fingerprint 均已填入且與實際檔案相符",
    "**exact-stream digest 交集為空**：對 `(goal, 64 個 canonical (entity,attr) 的有序 stream)` 算 canonical digest，assert `P0 corpus` 與 `MF0-C train/eval` 的 digest 集合**交集為空**，並另報各集合大小。⚠️ 只讓 episode-id 名稱不同**不夠** —— 這條堵的是「不同 ID 但其實同一 episode」的漏口（不保證分布獨立，但堵住重疊）"
  ],
  "authorization": {
    "granted": "**範圍 B（Codex [180]）**：補 manifest audit metadata（**不改 corpus**）＋ 實作並**跑一次** `P0` training，只取 final update 20,000 的 checkpoint",
    "forbidden_now": [
      "在此階段計算 `H`",
      "寫 controller training code",
      "碰 `W1` primary 成績",
      "看 train loss 後改 config／重跑／挑 checkpoint"
    ],
    "report_after": "只回報 fingerprint、固定的 train loss、assertion／manifest audit",
    "next": "H audit → 只有它過，才補完 sampling-law checklist → 才授權三 seed controller run"
  },
  "artifact_phase": {
    "status": "PASS（Codex [179] 範圍 A）",
    "P0_corpus": {
      "sha": "a4b73fa74b11ff17",
      "n_stream": 100000,
      "shape": [
        100000,
        65,
        2
      ],
      "dtype": "uint8"
    },
    "MF0C_artifact": {
      "sha": "9bc1848c9c84c4a1",
      "sizes": {
        "W0|train": 2000,
        "W0|eval": 300,
        "W1|train": 2000,
        "W1|eval": 300
      }
    },
    "exact_stream_digest": {
      "P0_distinct": 100000,
      "MF0C_distinct": 4300,
      "intersection": 0
    },
    "manifest": "experiments/mf0c_manifest.json"
  },
  "controller_compute_lock": {
    "conditional": "**conditional on `H` pass**；現在即寫入 prereg（Codex [180]）",
    "train_data": "controller **唯一可用**的是 `W1|train` 的 **2,000 frozen sessions**",
    "W0_train": "**不進任何 optimizer**；訓練完的**同一 policy** 另在 `W0|eval` 作 negative-transfer／leakage check",
    "primary_reading": "仍是 **`W1` eval**",
    "seeds": [
      2026081312,
      2026081313,
      2026081314
    ],
    "budget": "每 seed **20,000 updates × batch 32 = 640,000** sampled session rollouts",
    "index_schedule": "以 seed `2026081311` 從 `[0,1999]` **uniform-with-replacement** 產生並存 hash，**三個 seed 共用** —— 不得把 data redraw 混進 seed",
    "optimizer": "AdamW(lr=3e-4, betas=(0.9,0.999), **weight_decay=0**)",
    "clip": "global-grad-norm **1.0**",
    "forbidden": [
      "LR schedule",
      "entropy bonus",
      "reward／advantage normalization"
    ],
    "baseline": "僅**既鎖的 fixed leave-one-out baseline**",
    "checkpoint": "**final update 是唯一 checkpoint**",
    "claim_limit": "**不宣稱 20k 是最佳**，只是一次固定 compute budget。FAIL **只封存此 `MF0-C` 配方**，**不得**加步數／epoch／batch 再試"
  },
  "remaining_interface_blocker": {
    "what": "原本的「fixed-temperature weighted reservoir」目前**只是名稱**，必須寫成**可執行的單一 sampling law**",
    "must_specify": [
      "`τ` 的數值",
      "arrival 時採樣／eviction 的**機率與 log-prob**",
      "LOO baseline 的 **exact formula**",
      "action-RNG 的 seed rule",
      "GRU／affine 的**初始化規則**"
    ],
    "when": "**controller 訓練前**唯一剩餘的 interface blocker；**不得**等 `H` 或看 `P0` 之後才選",
    "note": "補寫這一項**不會**重開已鎖的 compute budget"
  },
  "sampling_law": {
    "note": "Codex [181]：**現在起草，不得等 `H`** —— 等 `H` 揭露 headroom 之後再選 `τ`／eviction law，那仍是可調入口",
    "priority": "`a_t = b + w_u^T u_t + w_h^T h_t`（controller 的單一 affine head）",
    "law": "**每個 arrival 一個 categorical action**。設當前 slot 集合 `S`（`|S| <= B`）：\n  · `|S| < B` → **確定性收入**，無隨機性、log-prob = 0\n  · `|S| = B` → 令候選 `C = S ∪ {new}`（`|C| = B+1`），**恰驅逐一個** `j ∈ C`，機率 `p(j) = softmax(-a/τ)[j]`\n  · 驅逐 `j = new` **等價於拒收** —— 因此**只會 evict 過去的 item 或拒收新的**，永遠維持恰 `B` 個 slot",
    "log_prob": "`log p(j) = -a_j/τ - logsumexp(-a_C/τ)`（**閉式**，可直接做 policy gradient）",
    "tau": "**τ = 1.0**（固定，不隨訓練變化，不做 sweep）",
    "eval_policy": "評估時改為**確定性** online top-B：以同一 `a_t` 取最大者保留、最小者驅逐；同分以事前固定的 canonical-key hash 打破",
    "init": "`GRUCell` 用 PyTorch 預設初始化（受 seed 控制）；**affine head 的 `w_u`、`w_h`、`b` 全部零初始化** → 初始 `a_t ≡ 0` → **policy 起點恰為均勻隨機驅逐**，是一個中性起點",
    "action_rng": "每個 rollout 的動作亂數由 `Generator(seed = hash((controller_seed, update_idx, rollout_idx)) mod 2**31)` 決定；**與資料 index schedule 的 RNG 分離**",
    "loo_baseline": "`R_k` 為第 `k` 個 rollout 的 future-query utility。**同一 session 內** `M` 個 rollout 的 leave-one-out：`b_k = (1/(M-1)) * Σ_{k'≠k} R_{k'}`；梯度為 `Σ_k (R_k - b_k) * Σ_t log p(action_t^k)`。**無 advantage normalization、無 entropy bonus**",
    "batch_grouping_PROPOSED": "⚠️ **這是我的提案，非既鎖規格**：batch 32 rollouts 拆成 **8 個 session × 每 session 4 個 rollout**（`M=4`）。理由：LOO baseline 必須在**同一 session 內**取，跨 session 取平均會把 session 難度差異當成 advantage。**請裁定 `M` 與分組方式**"
  }
}