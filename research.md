# 研究規劃：邏輯推論 / 記憶分離

最後更新：2026-07-31

---

## 1. 命題

**能力 ≈ 推理 × 知識**，而知識這一半可以外掛成可分離、可抽換的模組。

若成立，小骨幹就只需要擅長**推理**，靠外掛記憶補上知識，達成「越級打怪」——
以 7B/12B/27B 的邏輯能力運作，卻只背負其中極小一部分參數。

兩條軸，以及各自的**價格**：

| 軸 | 機制 | 價格 |
|---|---|---|
| **推理深度** | `use_looped_transformer` ✅ / ~~`use_dense_attention`~~ ❌ / `use_recurrence` | 參數 ~0；**VRAM 才是綁定約束** |
| **外掛知識** | `use_engram` | +105M 參數；offload 後 VRAM ~0 但慢 6.2× |
| **降低成本** | `use_latent_attention` | KV cache −25%、參數略減、速度略快 |

> **價格是一等公民。** 用 5 倍算力換到推理深度的機制，並沒有讓小模型變得有競爭力，
> 只是把成本從參數搬到算力。任何品質宣稱都必須附成本核算。

---

## 2. 已測量的結論

全部來自 `experiments/`，單一 seed，29M 骨幹（512d / 8 層 / 8 heads / 2 KV heads），RTX 4070。

### 2.1 Looped Transformer — 目前最強的正面結果

S₅ 置換合成，k=1..12，12000 步，兩組都從同一個 pretrain checkpoint 出發：

| k | 1–4 | 5 | 6 | 7 | 8 | 10 | 12 | 整體 | 參數 | VRAM |
|---|---|---|---|---|---|---|---|---|---|---|
| loop1（8 步） | 100% | 95.0% | 85.5% | 59.5% | 30.0% | 7.5% | 3.5% | 58.5% | 29.239M | 4.36 G |
| **loop2（16 步）** | **100%** | **100%** | **100%** | **100%** | **100%** | **100%** | **100%** | **100%** | 29.501M | 7.59 G |

**參數量幾乎相同，只多一圈純計算，天花板就從 k≈7 推到 >12 且毫無衰減。**
loop2 的真正天花板尚未找到（k=1..24 的延伸實驗進行中）。

### 2.2 CompleteAttention — 實測否定

| k | 1 | 3 | 6 |
|---|---|---|---|
| vanilla 個位數 | 99.3% | 92.5% | **93.3%** |
| dense 個位數 | 97.7% | 89.0% | **54.0%** |

不但沒買到深度，還**隨深度惡化**。

概念上的原因：跨層注意力是**稠密的 skip connection**，縮短有效路徑長度，
而非增加序列化計算 —— 它對準的量本來就不對。

成本也差：wall-clock 1.65×（seq 512），FLOP 倍數 `1 + attn佔比 × ((N+1)/2 − 1)`，
8 層/seq 512 是 1.49×，但 **24 層/seq 2048 會到 ~5.5×**。深度與上下文都惡化。

**建議：從主線降級成對照組。**

### 2.3 Engram — 證據不足，且指標選錯

pretrain perplexity 3.34 vs vanilla 3.28（4.6× 參數卻更差）。但 **pretrain perplexity
主要獎勵記憶** —— 正是 engram 該外包的那個軸 —— 所以這是弱證據，兩個方向都不能下結論。

真正的問題是**設計層面**：engram 以**表面 n-gram** 為 key，與模型當下的 hidden state 無關，
且以加性方式注入 residual stream。它是**靜態關聯表，不是命題要的 query-conditioned 動態記憶**。

### 2.4 成本的實測形狀

| 機制 | 參數 | VRAM / KV | 速度 |
|---|---|---|---|
| loop ×N | +0.26M（固定，共用適配器） | **+3.2 GiB / 圈** | 1/N |
| dense attention | 0 | +0.7 GiB | 0.61× |
| engram（GPU） | +105M | +1.7 GiB | 0.68× |
| engram（CPU offload） | +105M | ~0 | **0.16×** |
| latent attention | −1.3M | **KV −25%** | 1.07× |

**綁定約束是記憶體，不是參數。** 12 GB 的卡在 loop3 就 OOM（需梯度累積）。

---

## 3. 已撤回的結論（不要重蹈）

| 曾經寫下 | 為何錯 |
|---|---|
| 「天花板 k≈8，恰好等於層數」 | 混合運算任務 + 分布偏移的產物 |
| 「進位/狀態累積是真瓶頸」 | 十位數難是因為**多位數乘法**難；換成加減後立刻 ~100% |
| 「天花板 k≈3」 | **訓練不足**（6000 步 loss 0.59）；12000 步同一模型是 k≈7 |
| 「dense 造成 KV cache 膨脹」（readme §1.2 的前提） | dense 的 cache 與 vanilla **完全相同**；pool 每次 forward 重建 |

---

## 4. 方法論（本研究最貴的產出）

### 4.1 深度任務必須同時滿足兩個條件

| 任務 | 不可交換？ | 熵不塌縮？ | 結果 |
|---|---|---|---|
| 混合含乘法 `+-*` | ✅ | ❌ 3.08→0.62 bit | 純乘法正確率隨深度**上升**到 87% —— 猜比算划算 |
| 純加減 `+-` | ❌ 塌縮成 `v0+Σ±rhs` | ✅ | 求和是注意力一層的事（TC⁰）；8 層在 k=12 拿 90% |
| **S₅ 置換合成** | ✅ | ✅ 雙射保持均勻 | ← 正確的工具 |

理論保證：**加法在 TC⁰ 內**（固定深度可解），**S₅ 字問題是 NC¹-完備**
（固定深度證明上不可解）。這不是經驗法則，是可以事先推導的。

### 4.2 held-out 不要靠切分狀態空間

「train 用起始值 10–79、val 用 80–99」在混合運算下安全（乘法兩步就把值打散），
在純加減下**致命**：答案永遠黏在 v0 附近，模型記住訓練帶狀區域即可，
實測**訓練值域內 100%、值域外 0%**，而 train loss 只有 0.02。

**要 held-out 就排除具體樣本，不要切分狀態空間** —— 除非確認過動力學會混合。

### 4.3 天花板必須在收斂處量

6000 步 → k≈3，12000 步 → k≈7。同一個模型、同一個任務。
未收斂時量到的是訓練預算，不是架構極限。

### 4.4 聚合指標會藏掉全部訊號

逐位數拆開之前，vanilla 與 dense 看起來都是「~20%，對 k 平坦」。
拆開後才看到個位數 99%→93% vs 97%→54%，十位數兩者都貼著基準。
**任何合成任務都要拆出可分離的子指標。**

### 4.5 其他已付過學費的陷阱

- **`use_engram` 預設 `True`** —— 沒寫死旗標的「vanilla」會偷帶 engram（134M 而非 29M）
- **6400 BPE 詞表的數字切法不一致**（42 個單 token、58 個雙 token）—— 改成空白分隔位數後，正確率 3.7%→11.3% 且步數少 60%
- **weight decay 要分組** —— 105M 列的雜湊表每步只有 0.4% 拿到梯度，統一 decay 會把未造訪的列拉向 0
- **中文數學的答案抽取不可靠** —— 「取最後一個數字」四題只對一題（餘數、多部分答案）
- **推論路徑才是 bug 的溫床** —— 本 repo 24 個 bug 幾乎全在增量解碼路徑，訓練路徑大多正確
- **中止實驗時要清掉對應的等待迴圈**，否則會留下永遠不會觸發的殭屍

---

## 5. 各機制現況

| 機制 | 狀態 | 下一步 |
|---|---|---|
| `use_looped_transformer` | ✅ 已補上可變圈數 / input injection / loop-index 嵌入 / 隨機圈數 | E1 進行中 |
| `use_dense_attention` | ❌ 實測否定 | 降級為對照組 |
| `use_engram` | ⚠️ 可用但設計有缺（靜態、表面 key、加性注入） | 改成 query-conditioned |
| `use_latent_attention` | ✅ 已修（cache 768→192，比 GQA 小 25%） | 與 loop 組合以壓 VRAM |
| `use_recurrence` | ⚠️ 訓練時實質失效（無連續 segment 資料流） | 低優先 |
| 自適應深度 | 📐 已規劃，未實作 | 見 `PLAN_looped.md` |

---

## 6. 目標架構

```
┌─ 小骨幹 ────────────────────────────────┐
│  Prelude (2層, 跑1次)                    │
│  ┌─ Core (4層) ──── 迴圈 k 次 ─────┐     │  ← 深度來自迭代
│  │   ↑ 每圈注入 input embedding     │     │
│  │   ↑ 跨迭代狀態向量               │     │  ← 解決累積型計算
│  │   ↓ core 首層判斷「還要不要再想」 │     │  ← 自適應深度，零額外成本
│  │   ↓ 每圈可查詢記憶               │     │
│  └──────────────┬──────────────────┘     │
│  Coda (1層, 跑1次) ← 保持薄               │
└─────────────────┼───────────────────────┘
                  │ query-conditioned retrieval
            ┌─────▼─────┐
            │ 外掛記憶庫 │  ← 可放 RAM、可抽換、可增量寫入
            └───────────┘
```

三個設計決定（理由見 `PLAN_looped.md`）：

1. **深度來自迭代，不是連通性** —— 用 loop，丟掉 CA
2. **迭代間要有顯式狀態** —— 純注意力每層都要重建狀態
3. **記憶要 query-conditioned** —— 以 hidden state 為 key，而非 token 字面；
   介面用 cross-attention 而非加性注入（RETRO 是較近的參照點）

**自適應深度採 per-token，不是 per-sequence。** 隱式深度不會取代顯式 CoT，
而 CoT 裡多數 token 是語法填充（標點、連接詞），不需要深思 ——
per-sequence 會被序列裡最難的那個 token 拖住，等於放棄全部收益。

**停止訊號放 core 首層**（零額外成本、判斷可餵給下一圈），
並用**免訓練的 KL 收斂判準當老師**去監督它，繞開 ACT 的 ponder-cost 不穩定性。

---

## 7. 實驗排程

### 進行中

**E1 — 迴圈能否推高深度天花板**（k=1..24，四組，約 4 小時）
- 已知：loop2 在 k≤12 全對，loop1 從 k=7 崩壞
- 待答：loop2 的天花板在哪；loop3/4 是否繼續推進；**線性還是次線性**
- loop2/3/4 參數量完全相同，loop1 與 vanilla 位元等價 → 無參數量混淆

### 接下來

| # | 實驗 | 前置 | 成本 |
|---|---|---|---|
| **E2** | test-time compute scaling：隨機圈數訓練，推論用 1/2/4/8 圈 | 已就緒 | ~1.5h |
| **E3** | 跨迭代狀態向量能否解決累積型計算 | 需實作狀態通道 | ~2h |
| **E4** | 成本核算（每個實驗都附） | — | 併入 |
| **E5** | 自適應深度的 Pareto 曲線 | KL 判準 + per-token 階段一 | ~2h |
| **E6** | 圈數 vs 真實難度 k；圈數 vs 詞性 | 同上 | ~1h |
| **M1** | Engram 改 query-conditioned，在推理任務上對照 | 需重寫 stage1/2 | ~4h |

**E2 是最有機會出漂亮結果的**（同一組權重、不同推論預算、不同能力 = 越級打怪的直接證據）。
**E6-2（詞性 vs 圈數）預計是整條線最有說服力的一張圖** —— 展示模型學會了在標點上不思考、
在數字上深思。這只有本測試台做得到，因為難度是可控且已知的。

---

## 8. 預測（先寫下來，避免事後合理化）

1. **E1 走向線性**（loop4 解到 k≈28）機率中等偏高。已知 2 圈 ≥1.7× 且未飽和。
   反面：29M 的寬度可能先綁住。
2. **E2 會成立**，且能外推到訓練沒見過的圈數（正弦 loop-index 嵌入的設計目的）。
3. **純迴圈不會解決累積型計算** —— 需要顯式狀態通道。
4. **VRAM 會持續是實務限制**，比參數量早得多。`latent + core-only 迴圈` 是主要對策。
5. **Engram 現況（靜態表面 key）在推理任務上不會有幫助**；要有幫助必須先改成 query-conditioned。

---

## 9. 未解的風險

- **單一 seed。** 所有結論都需要 2–3 個 seed 複驗才算穩。目前只有效果極大的（loop2 的 58.5%→100%）可以放心。
- **29M 規模遠低於文獻。** RETRO 最小 150M，recurrent-depth 用 3.5B。
  合成任務上的結論不保證轉移到自然語言。
- **合成任務 vs 真實推理的落差。** S₅ 置換是乾淨的深度量尺，但它不是 GSM8K。
  最終仍需在真實推理資料上驗證，而 29M 在 GSM8K 上大概率是 0%（見 `FIX_TODO.md` R2/R3）。
- **記憶軸幾乎還沒開始。** 目前所有進展都在推理軸。命題的另一半仍是空白。

---

## 附：關鍵檔案

| 檔案 | 內容 |
|---|---|
| `CLAUDE.md` | 給 AI 助手的操作指南 + 已測發現 + 陷阱 |
| `experiments/PLAN_looped.md` | Looped transformer 的實作與實驗細節規劃 |
| `FIX_TODO.md` | 24 個已修 bug 的審計記錄 + 3 個推理管線的未修項 |
| `experiments/synth_depth_task.py` | 深度任務（S₅ 置換合成） |
| `experiments/run_reasoning_sft.py` | 從共用 checkpoint 出發的推理 SFT |
| `experiments/eval_reasoning_quant.py` | 量化推理評測（CoT loss + exact-match + Wilson CI） |
| `test/test_bugfix_regressions.py` | 55 個回歸測試，與 `FIX_TODO.md` 一一對應 |
